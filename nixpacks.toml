[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]
# Environment variables are available at runtime, not during build
# PINATA_JWT is only used in API routes (runtime), not needed during build

[start]
cmd = "npm start"

